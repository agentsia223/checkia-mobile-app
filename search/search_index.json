{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"Check-IA Mobile <p>       Expo and React Native client for the Check-IA fact-checking platform, built for fast mobile verification workflows and public open-source contribution.     </p> Set up the app Explore architecture Client boundaries <ul> <li>Expo Router screens</li> <li>Supabase Auth sessions</li> <li>Check-IA API requests</li> <li>Jest and React Native tests</li> </ul>"},{"location":"#mobile-workflows","title":"Mobile Workflows","text":"Local setup Run with Expo <p>Install dependencies, configure environment variables, and launch Android, iOS, web, or Expo Go targets.</p> API contract Connect to Check-IA <p>Understand backend routes for text, URL, image, history, facts, and keyword workflows.</p> Quality Test the client <p>Run TypeScript, Jest, React Native Testing Library, and focused service-layer checks before opening a PR.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Text and URL Verification - Submit claims or URLs to the Check-IA backend and poll asynchronous analysis results.</li> <li>Image Verification - Detect AI-generated images and verify image content through backend image workflows.</li> <li>Verification History - Review synchronized text, URL, and image verification history for the signed-in user.</li> <li>Learning Area - Browse media literacy content written for French-speaking communities.</li> <li>Secure Session Storage - Store Supabase sessions with <code>expo-secure-store</code>.</li> </ul>"},{"location":"#tech-stack","title":"Tech Stack","text":"Layer Technology Runtime Expo SDK 54, React Native 0.81, React 19 Navigation Expo Router Language TypeScript Auth Supabase Auth, Expo SecureStore API Client Axios Testing Jest, jest-expo, React Native Testing Library CI GitHub Actions, Codecov-ready coverage"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li>Getting Started - Set up the mobile app locally.</li> <li>Architecture - Understand the Expo Router, component, hook, and service-layer structure.</li> <li>Backend API Contract - Review the endpoints used by the mobile app.</li> <li>Contributing - Prepare issues, pull requests, and documentation updates.</li> <li>Testing - Run and extend the mobile test suite.</li> <li>README - Return to the GitHub repository overview.</li> </ul>"},{"location":"ARCHITECTURE/","title":"Architecture","text":"<p>Check-IA Mobile is organized around Expo Router routes, reusable feature components, and a small API service layer.</p>"},{"location":"ARCHITECTURE/#routing","title":"Routing","text":"<p>Routes live in <code>app/</code> and are grouped by user flow:</p> <ul> <li><code>app/(auth)/</code> contains authentication and onboarding screens.</li> <li><code>app/(tabs)/</code> contains the main tab navigation screens.</li> <li><code>app/history/</code> contains the history view.</li> <li><code>app/learn/[slug].tsx</code> displays learning content by slug.</li> <li><code>app/result/[id].tsx</code> displays verification results.</li> </ul>"},{"location":"ARCHITECTURE/#feature-areas","title":"Feature Areas","text":"<ul> <li><code>components/home/</code> renders the dashboard summary and recent history UI.</li> <li><code>components/verify/</code> renders the text, URL, image, and audio verification inputs.</li> <li><code>components/learn/</code> renders learning content lists and cards.</li> <li><code>components/profile/</code> renders profile menu rows.</li> <li><code>components/ui/</code> contains reusable primitive UI components.</li> </ul>"},{"location":"ARCHITECTURE/#data-and-state","title":"Data and State","text":"<ul> <li><code>hooks/useVerify.ts</code> owns local state and actions for the verification screen.</li> <li><code>utils/apiMappers.ts</code> maps backend submissions and image verification records into shared view models for history and result screens.</li> <li><code>data/</code> contains static learning content and fallback UI data used by current screens.</li> <li><code>constants/</code> contains theme values and feature constants.</li> <li><code>utils/</code> contains testable helper functions.</li> </ul>"},{"location":"ARCHITECTURE/#api-layer","title":"API Layer","text":"<p><code>services/supabase.ts</code> creates the Supabase client and persists Supabase Auth sessions in <code>expo-secure-store</code>. <code>services/api.ts</code> creates the Axios client and exports API modules for authentication, fact-checking, image verification, task polling, public content, and local URL preview metadata.</p> <p>The backend base URL comes from:</p> <pre><code>EXPO_PUBLIC_BACKEND_URL\n</code></pre> <p><code>EXPO_PUBLIC_API_URL</code> is accepted for older local environments and normalized back to the backend root when it ends in <code>/api</code>.</p> <p>Supabase Auth reads:</p> <pre><code>EXPO_PUBLIC_SUPABASE_URL\nEXPO_PUBLIC_SUPABASE_ANON_KEY\n</code></pre> <p>The Axios request interceptor reads the active Supabase session through the SDK and adds <code>Authorization: Bearer &lt;supabase_jwt&gt;</code> when a token is available. On a 401 response, it asks Supabase to refresh the session, retries the request once, and signs the user out if the retry cannot be authorized.</p> <p>Authentication uses Supabase SDK methods (<code>signUp</code>, <code>signInWithPassword</code>, <code>signOut</code>). The app must not call backend <code>/api/auth/*</code> proxy endpoints.</p> <p>Current backend routes used by the app:</p> <ul> <li><code>POST /api/submissions/</code></li> <li><code>GET /api/task-status/{taskId}/</code></li> <li><code>GET /api/user-submissions/</code></li> <li><code>POST /api/detect-ai-image/</code></li> <li><code>POST /api/verify-image-content/</code></li> <li><code>GET /api/image-verifications/</code></li> <li><code>GET /api/facts/</code></li> <li><code>GET /api/keywords/</code></li> </ul> <p>Text and URL submissions poll <code>/api/task-status/{taskId}/</code> until the backend status leaves <code>en cours</code>. Image submissions poll the same task endpoint until the asynchronous image verification returns a final result. API code preserves backend French status strings (<code>en cours</code>, <code>v\u00e9rifi\u00e9</code>, <code>rejet\u00e9</code>) and maps them to UI copy only in <code>utils/apiMappers.ts</code>.</p>"},{"location":"ARCHITECTURE/#testing","title":"Testing","text":"<p>Tests are colocated in <code>__tests__</code> directories near the source they cover. Jest uses the <code>jest-expo</code> preset and React Native Testing Library for component tests.</p>"},{"location":"DEVELOPMENT/","title":"Development Guide","text":""},{"location":"DEVELOPMENT/#local-backend","title":"Local Backend","text":"<p>The mobile app expects a backend API compatible with the routes in <code>services/api.ts</code>.</p> <p>Set the backend and Supabase project values in <code>.env</code>. The app fails fast on startup when these values are missing outside tests:</p> <pre><code>EXPO_PUBLIC_BACKEND_URL=http://localhost:8000\nEXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co\nEXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key\n</code></pre> <p><code>localhost</code> works only when the app runtime can reach a backend on the same host. For physical device testing, <code>localhost</code> points to the device, not your computer. Use a LAN IP address or a tunnel URL that you control:</p> <pre><code>EXPO_PUBLIC_BACKEND_URL=http://192.168.1.20:8000\n</code></pre> <p>Do not commit local tunnel URLs.</p> <p>The current mobile client signs users in directly with Supabase Auth and sends the Supabase access token as <code>Authorization: Bearer &lt;supabase_jwt&gt;</code> to the backend. It does not call the backend <code>/api/auth/*</code> proxy views.</p> <p>The current mobile client calls these backend capabilities:</p> <ul> <li>Text and URL submissions through <code>POST /api/submissions/</code>, with task polling on <code>/api/task-status/{taskId}/</code>.</li> <li>User text history through <code>GET /api/user-submissions/</code>.</li> <li>Image AI detection through <code>POST /api/detect-ai-image/</code>.</li> <li>Image content verification through <code>POST /api/verify-image-content/</code>.</li> <li>Image history through <code>GET /api/image-verifications/</code>.</li> <li>Public facts through <code>GET /api/facts/</code>.</li> <li>Public keywords through <code>GET /api/keywords/</code>.</li> </ul> <p>Audio verification is still UI-only and intentionally disabled in the analyze button until a backend contract is added.</p>"},{"location":"DEVELOPMENT/#useful-commands","title":"Useful Commands","text":"<pre><code>npm start\nnpm run android\nnpm run ios\nnpm run web\nnpm test\nnpm run coverage\nnpm run typecheck\n</code></pre>"},{"location":"DEVELOPMENT/#troubleshooting","title":"Troubleshooting","text":"<p>Clear Expo and Metro state:</p> <pre><code>npx expo start -c\n</code></pre> <p>Reinstall dependencies:</p> <pre><code>rm -rf node_modules\nnpm install\n</code></pre> <p>If native builds become inconsistent, regenerate native projects through Expo instead of committing local generated <code>ios/</code> or <code>android/</code> folders unless the project intentionally moves to a prebuild workflow.</p>"},{"location":"architecture/","title":"Architecture Overview","text":"<p>Check-IA Mobile is organized around Expo Router routes, reusable feature components, and a small API service layer.</p>"},{"location":"architecture/#system-diagram","title":"System Diagram","text":"Editable source: mobile-architecture.excalidraw"},{"location":"architecture/#main-areas","title":"Main Areas","text":"<ul> <li><code>app/</code> contains Expo Router routes and screens.</li> <li><code>components/</code> contains reusable UI and feature components.</li> <li><code>hooks/</code> contains shared state and behavior.</li> <li><code>services/</code> contains Supabase and Check-IA API clients.</li> <li><code>utils/</code> contains testable mapping and helper functions.</li> <li><code>data/</code> contains static learning content and fallback dashboard data.</li> </ul> <p>For the implementation-level overview, see Mobile Client.</p> <p>For backend endpoints used by the app, see Backend API Contract.</p>"},{"location":"architecture/backend-api/","title":"Backend API Contract","text":"<p>Check-IA Mobile connects to the main Check-IA backend through authenticated REST endpoints.</p>"},{"location":"architecture/backend-api/#authentication","title":"Authentication","text":"<p>The app signs in, signs up, refreshes, and signs out through the official Supabase SDK. It sends the active Supabase access token to the backend:</p> <pre><code>Authorization: Bearer &lt;supabase_jwt&gt;\n</code></pre> <p>The app must not call backend <code>/api/auth/*</code> proxy endpoints.</p>"},{"location":"architecture/backend-api/#routes","title":"Routes","text":"Method Endpoint Purpose <code>POST</code> <code>/api/submissions/</code> Submit text or URL verification <code>GET</code> <code>/api/task-status/{taskId}/</code> Poll asynchronous task status <code>GET</code> <code>/api/user-submissions/</code> Get authenticated text and URL history <code>POST</code> <code>/api/detect-ai-image/</code> Detect AI-generated images <code>POST</code> <code>/api/verify-image-content/</code> Verify an image against a claim <code>GET</code> <code>/api/image-verifications/</code> Get authenticated image history <code>GET</code> <code>/api/facts/</code> Read public verified facts <code>GET</code> <code>/api/keywords/</code> Read public keywords <p>Text and URL submissions poll <code>/api/task-status/{taskId}/</code> until the backend status leaves <code>en cours</code>. Image submissions use the same task endpoint until the asynchronous image verification returns a final result.</p> <p>API code preserves backend French status strings such as <code>en cours</code>, <code>v\u00e9rifi\u00e9</code>, and <code>rejet\u00e9</code>, then maps them to UI copy through shared utilities.</p>"},{"location":"contributing/","title":"Contributing","text":"<p>Contributions should follow the process in the repository Contributing Guide.</p>"},{"location":"contributing/#expectations","title":"Expectations","text":"<ul> <li>Keep pull requests focused on one change.</li> <li>Add or update tests for behavior changes.</li> <li>Update documentation when setup, scripts, API expectations, or user-facing behavior changes.</li> <li>Include screenshots or recordings for visible UI changes.</li> <li>Keep private URLs, credentials, tokens, and signing material out of the repository.</li> </ul>"},{"location":"contributing/#required-checks","title":"Required Checks","text":"<pre><code>npm run typecheck\nnpm test\n</code></pre> <p>For UI changes, also run the app on Android, iOS, web, or Expo Go as relevant.</p>"},{"location":"contributing/code-of-conduct/","title":"Code of Conduct","text":"<p>Check-IA Mobile follows the repository Code of Conduct.</p> <p>Report Code of Conduct concerns privately to hello@check-ia.app.</p> <p>For maintainer handling steps, see Incident Response.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Check-IA Mobile runs with Expo and connects to a Check-IA-compatible backend plus Supabase Auth.</p>"},{"location":"getting-started/#requirements","title":"Requirements","text":"<ul> <li>Node.js 20.19.4 or newer.</li> <li>npm 10 or newer.</li> <li>Git.</li> <li>Expo CLI through <code>npx expo</code>.</li> <li>Android Studio, Xcode, Expo Go, or another Expo-compatible test target.</li> <li>Watchman on macOS or Linux is recommended.</li> </ul>"},{"location":"getting-started/#setup-flow","title":"Setup Flow","text":"<ol> <li>Clone the repository.</li> <li>Install dependencies with <code>npm install</code>.</li> <li>Copy <code>.env.example</code> to <code>.env</code>.</li> <li>Set backend and Supabase values.</li> <li>Start Expo with <code>npm start</code>.</li> </ol> <p>See Mobile Setup and Environment Variables for details.</p>"},{"location":"getting-started/environment/","title":"Environment Variables","text":"<p>The mobile app reads public Expo runtime configuration from <code>.env</code>.</p> <pre><code>EXPO_PUBLIC_BACKEND_URL=http://localhost:8000\nEXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co\nEXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key\n</code></pre>"},{"location":"getting-started/environment/#backend-url","title":"Backend URL","text":"<p><code>EXPO_PUBLIC_BACKEND_URL</code> should point to the Check-IA backend root, not the <code>/api</code> path.</p> <p><code>EXPO_PUBLIC_API_URL</code> is still accepted for older local environments and is normalized back to the backend root if it ends in <code>/api</code>.</p> <p>For physical device testing, <code>localhost</code> points to the device, not your development machine. Use a LAN IP address or a tunnel URL that you control:</p> <pre><code>EXPO_PUBLIC_BACKEND_URL=http://192.168.1.20:8000\n</code></pre> <p>Do not commit private tunnel URLs.</p>"},{"location":"getting-started/environment/#supabase","title":"Supabase","text":"<p>The app signs users in directly with Supabase Auth and stores sessions through <code>expo-secure-store</code>.</p> <p>Do not point the app at backend <code>/api/auth/*</code> proxy endpoints. The backend validates Supabase JWTs sent by the app:</p> <pre><code>Authorization: Bearer &lt;supabase_jwt&gt;\n</code></pre>"},{"location":"getting-started/environment/#sensitive-data","title":"Sensitive Data","text":"<p>Do not commit:</p> <ul> <li><code>.env</code> files.</li> <li>API keys or service tokens.</li> <li>Native signing keys, certificates, or provisioning profiles.</li> <li>Production analytics or crash reporting credentials.</li> <li>Private tunnel URLs.</li> </ul>"},{"location":"getting-started/mobile/","title":"Mobile Setup","text":"<p>Clone the repository and install dependencies:</p> <pre><code>git clone https://github.com/agentsia223/checkia-mobile-app.git\ncd checkia-mobile-app\nnpm install\n</code></pre> <p>Create a local environment file:</p> <pre><code>cp .env.example .env\n</code></pre> <p>Start Expo:</p> <pre><code>npm start\n</code></pre> <p>Choose a target from the Expo prompt:</p> <ul> <li>Press <code>a</code> for Android.</li> <li>Press <code>i</code> for iOS on macOS.</li> <li>Press <code>w</code> for web.</li> <li>Scan the QR code with Expo Go for a physical device.</li> </ul>"},{"location":"getting-started/mobile/#useful-commands","title":"Useful Commands","text":"<pre><code>npm start\nnpm run android\nnpm run ios\nnpm run web\nnpm run typecheck\nnpm test\nnpm run coverage\n</code></pre>"},{"location":"getting-started/mobile/#troubleshooting","title":"Troubleshooting","text":"<p>Clear Expo and Metro state:</p> <pre><code>npx expo start -c\n</code></pre> <p>Reinstall dependencies:</p> <pre><code>rm -rf node_modules\nnpm install\n</code></pre> <p>If native builds become inconsistent, regenerate native projects through Expo instead of committing local generated <code>ios/</code> or <code>android/</code> folders unless the project intentionally moves to a prebuild workflow.</p>"},{"location":"testing/","title":"Testing Overview","text":"<p>Check-IA Mobile uses Jest, jest-expo, React Native Testing Library, and TypeScript checks.</p>"},{"location":"testing/#required-local-checks","title":"Required Local Checks","text":"<pre><code>npm run typecheck\nnpm test\n</code></pre>"},{"location":"testing/#coverage","title":"Coverage","text":"<pre><code>npm run coverage\n</code></pre> <p>Coverage output is generated locally and ignored by Git. CI uploads coverage to Codecov when the repository has <code>CODECOV_TOKEN</code> configured.</p> <p>For the full QA process, see the repository Test Plan.</p>"},{"location":"testing/mobile/","title":"Mobile Tests","text":"<p>Tests are colocated in <code>__tests__</code> directories near the source they cover.</p>"},{"location":"testing/mobile/#areas-covered","title":"Areas Covered","text":"<ul> <li>App route smoke tests.</li> <li>Auth screens and tab screens.</li> <li>Home, learn, verify, profile, and UI components.</li> <li>Verification and current user hooks.</li> <li>API services and helpers.</li> <li>Constants and mapper behavior through consumers.</li> </ul>"},{"location":"testing/mobile/#commands","title":"Commands","text":"<pre><code>npm test\nnpm run test:watch\nnpm run coverage\n</code></pre> <p>For changes that affect screens, run at least one Expo target before requesting review:</p> <pre><code>npm run android\n# or\nnpm run ios\n# or\nnpm run web\n</code></pre>"}]}